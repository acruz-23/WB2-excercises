// c to f (c *9/5) + 32
//variables
var celsius;

//sample data
celsius = 32;

//calc
fahrenheit = (celsius * 9 / 5) + 32;

//output 
console.log( celsius, "degrees celsius", fahrenheit, "degrees fahrenheit is")
